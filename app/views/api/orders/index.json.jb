@orders.map do |order|
  {
    id: order.id,
    user_id: order.user_id,
    product: order.product, #product for the front end
    quantity: order.quantity,
    subtotal: order.subtotal,
    tax: order.tax,
    total: order.total,
  }
end
